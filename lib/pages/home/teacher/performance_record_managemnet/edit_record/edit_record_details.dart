import 'package:flutter/material.dart';

class EditRecordDetailPage extends StatelessWidget {
  const EditRecordDetailPage({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Edit Record Detail"),
      )
    );
  }
}